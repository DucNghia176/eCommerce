server:
  port: 8085

spring:
  application:
    name: api-gateway
  main:
    web-application-type: reactive
  data:
    redis:
      host: localhost
      port: 6379
      password: 123456aA@
      connect-timeout: 60000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    ecommerce.gateway.filter.JwtGlobalFilter: INFO
